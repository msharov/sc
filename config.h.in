// SC is free software distributed under the MIT license
// config.h generated by configure
#pragma once

// Define to the one symbol short name of this package.
#define SC_NAME		"@PKG_NAME@"
// Define to the version of this package.
#define SC_VERSION	@PKG_VERSION@
// Define to the version string of this package.
#define SC_VERSTRING	"@PKG_VERSTR@"
// Define to the address where bug reports for this package should be sent.
#define SC_BUGREPORT	"@PKG_BUGREPORT@"

#define DFLT_PAGER	"@PAGER@"
#define LIBDIR		"@libdir@"
#define DIESAVEPATH	"/tmp/" SC_NAME ".sav"
#define HISTORY_FILE	"/home/%s/.config/sc_history"

#define UNUSED		__attribute__((unused))

// Define to 1 if you have curses.h
#undef HAVE_CURSES_H
#if !HAVE_CURSES_H
    #error "You must have the ncurses library to compile @PKG_NAME@: https://www.gnu.org/software/ncurses/ncurses.html"
#endif

#define DONT_HAVE_LIBRARY	0
#define lncurses		(-1)
#define HAVE_LIBNCURSES		DONT_HAVE_LIBRARY @libncurses@
#if HAVE_LIBNCURSES == DONT_HAVE_LIBRARY
    #error "Could not find the ncurses library, required to build this package. If it is not provided by your distribution, you can get it at https://www.gnu.org/software/ncurses/ncurses.html. If you already installed it, make sure libncurses.so can be found in /lib, /usr/lib, or $LD_LIBRARY_PATH"
#endif
#undef lncurses
